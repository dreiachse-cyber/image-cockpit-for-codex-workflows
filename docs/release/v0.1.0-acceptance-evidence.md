# v0.1.0 Acceptance Evidence

更新日: 2026-06-23

This document maps the private MVP requirements to the current evidence so the owner review can focus on the four primary workflows and the local-first boundary.

## Scope

- Candidate: `0.1.0-rc.1`
- Branch: `codex/mvp-local-cockpit`
- Draft PR: https://github.com/dreiachse-cyber/image-cockpit-for-codex-workflows/pull/1
- Repository visibility: private until owner approval
- Product boundary: local app, local handoff files, no direct OpenAI API calls from the app
- Owner review guide: `docs/release/v0.1.0-owner-review.md`
- Final audit: `docs/release/v0.1.0-final-audit.md`
- Owner decision record: `docs/release/v0.1.0-owner-decision.md`

## Acceptance Matrix

| Requirement | Acceptance expectation | Evidence | Status |
| --- | --- | --- | --- |
| Image generation | Guided Start exposes image generation, creates a prompt-focused Codex handoff job, and does not attach edit-only source assets or annotations. | `src/App.tsx`, `server/index.ts`, `scripts/smoke.mjs`, `docs/review/mvp-review-report.md` | Accepted for private MVP |
| Image editing | User can select an image, add notes and annotations, then create a Codex handoff job that includes selected source asset and edit context. | `src/App.tsx`, `server/index.ts`, `scripts/smoke.mjs`, `docs/qa/image-edit-handoff-1440x1024.png` | Accepted for private MVP |
| Sprite sheet generation | User can import an image, split it with grid controls, populate frame/timeline state, and preserve sprite grid context in handoff payloads. | `src/App.tsx`, `scripts/smoke.mjs`, `docs/demo/mvp-demo.gif`, `docs/review/mvp-review-report.md` | Accepted for private MVP |
| Sprite sheet editing | User can edit frame size, timeline order, anchor, chroma key cleanup, export PNG / ZIP / GIF / metadata, and preserve sprite edit context in handoff payloads. | `src/App.tsx`, `scripts/smoke.mjs`, `docs/qa/sprite-edit-simple-1440x1024.png`, `docs/qa/sprite-edit-mobile-390x844.png` | Accepted for private MVP |
| Guided Start simplicity | The first choice is the four primary workflows, lower-priority detailed controls are not the default focus, and simplified action rows avoid duplicate import commands. | `src/App.tsx`, `scripts/ui-smoke.mjs`, `docs/roadmap/release-roadmap.md`, `docs/qa/guided-start-1440x1024.png`, `docs/qa/simple-sprite-generate-actions-1280x720.png` | Accepted for private MVP |
| Core localization | Japanese / English language selection covers the core workflow labels, canvas controls, sprite controls, export actions, and browser-locale default. | `src/App.tsx`, `src/App.test.ts`, `scripts/release-audit.mjs`, `docs/release/v0.1.0-checklist.md` | Accepted for private MVP |
| Local-first boundary | The app does not depend on OpenAI API keys, direct OpenAI API calls, model weights, or cloud-hosted image generation. | `README.md`, `.env.example`, `scripts/release-audit.mjs`, `CONTRIBUTING.md` | Accepted for private MVP |
| Codex handoff | Jobs are written under `codex-handoff/inbox/`, selected assets are copied under `codex-handoff/assets/`, and returned images are read from `codex-handoff/outbox/`. | `server/index.ts`, `scripts/smoke.mjs`, `docs/usage/manual-handoff.md` | Accepted for private MVP |
| Pending job lock | After a Codex job is created, the job button cannot create another job until the pending state is resolved by a result, runner failure, unavailable state, completed-without-image state, or orphaned unknown status. | `src/App.tsx`, `src/App.test.ts`, `scripts/smoke.mjs`, `docs/release/v0.1.0-checklist.md` | Accepted for private MVP |
| Manual handoff fallback | When `codex exec` cannot be launched, the UI unlocks, points the user to return an outbox image, keeps `Import Latest` visible, and imports a returned outbox image through Local Inbox. | `src/App.tsx`, `docs/usage/manual-handoff.md`, `scripts/release-audit.mjs`, `docs/qa/simple-image-generate-import-latest-1280x720.png`, `docs/qa/simple-image-generate-import-latest-mobile-390x844.png`, `docs/qa/manual-handoff-import-latest-1280x720.png` | Accepted for private MVP |
| Runner lifecycle wiring | A mock autorun runner can pass preflight as `ready`, receive the handoff prompt and runner environment, move a job from `running` to `completed`, write a PNG to outbox, and have that PNG imported through the Local Inbox endpoint. | `server/index.ts`, `scripts/smoke.mjs`, `scripts/release-audit.mjs` | Accepted for private MVP wiring only |
| Codex command diagnostics | Doctor and runner preflight report the requested command, selected launch command, and resolved command paths. On this Windows review environment, the WindowsApps desktop shim is blocked, while `%LOCALAPPDATA%\OpenAI\Codex\bin\...\codex.exe --help` succeeds. | `server/index.ts`, `scripts/doctor.mjs`, `scripts/smoke.mjs`, `scripts/release-audit.mjs` | Accepted for private MVP diagnostics only |
| Real Codex runner smoke | The installed Codex CLI is launched by the local handoff server, reads the handoff job, writes a Markdown sidecar to outbox, and exits 0. | `scripts/real-codex-runner-smoke.mjs`, `docs/qa/real-codex-runner-smoke.md`, `server/index.ts` | Verified for no-image runner completion |
| Release readiness | Required docs, release gates, CI workflow, local verification commands, and tracked-file safety checks are present. | `.github/workflows/ci.yml`, `scripts/doctor.mjs`, `scripts/release-audit.mjs`, `docs/release/v0.1.0-runbook.md` | Accepted for private MVP |
| Owner review path | The owner has a short review entrypoint covering `review:local`, manual workflow checks, decision files, and approval gates. | `docs/release/v0.1.0-owner-review.md`, `README.md`, `docs/release/v0.1.0-checklist.md` | Accepted for private MVP |
| Final audit | The completion definition and explicit user requirements are mapped to evidence, with remaining items separated as owner approval gates. | `docs/release/v0.1.0-final-audit.md`, `docs/roadmap/release-roadmap.md`, `docs/release/v0.1.0-owner-decision.md` | Accepted for private MVP |

## Verification Commands

The release candidate verification path is:

```powershell
npm run verify
```

Expanded verification path:

```powershell
npm run doctor
npm run typecheck
npm test
npm run build
npm run smoke
npm run release:audit
```

Optional browser review smoke:

```powershell
npm run ui:smoke
```

Optional real Codex runner smoke when a runnable local Codex CLI is available:

```powershell
npm run codex:smoke
```

Full owner-review local sweep on a Codex-installed review machine:

```powershell
npm run review:local
```

Expected result: all commands pass. `npm run doctor` may warn that the Codex executable is unavailable in environments where Windows blocks `codex exec`; that is acceptable for private MVP review when manual handoff is being reviewed. On this Windows review environment, `npm run doctor` selects the terminal-runnable Codex CLI under `%LOCALAPPDATA%\OpenAI\Codex\bin\...` and verifies `--help` successfully.

## GitHub Evidence

- Draft PR: https://github.com/dreiachse-cyber/image-cockpit-for-codex-workflows/pull/1
- CI workflow: `.github/workflows/ci.yml`
- Required CI path: `npm run verify` covering doctor, typecheck, test, build, smoke, and release audit

## Remaining Gates

The following are not completed by this document and still require explicit owner decision:

- Owner review of `docs/review/mvp-review-report.md`
- Owner review of `docs/release/v0.1.0-owner-decision.md`
- Approval of `docs/release/v0.1.0-release-notes.md`
- Approval to merge into `main`
- Approval to change repository visibility from private to public
- Approval to create the `v0.1.0` tag and GitHub release

## Do Not Treat As Accepted

- Direct OpenAI API integration from the app
- Required `OPENAI_API_KEY` setup
- ComfyUI, AUTOMATIC1111, Replicate, or other external adapters
- IndexedDB persistence
- Game-engine-specific exports
- Bundled model weights or license-unclear assets
