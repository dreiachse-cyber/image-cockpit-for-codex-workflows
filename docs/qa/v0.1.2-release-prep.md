# v0.1.2 Release Prep QA

Date: 2026-06-29
Branch: `main`
Status: release prep verification passed

## Scope

This QA note covers the v0.1.2 release-prep update for version, release notes, changelog, release audit, and tag-readiness documentation.

Target commit before release-prep changes: `91d75e4` (`Add running Codex job progress indicators`).

Release scope decision:

- Use the latest main stabilization scope after `v0.1.1`.
- Include settings recovery reporting, detached direction-split recovery, animation result quality gating, first-two usable candidate adoption, animation delivery reliability SLO evidence, and running job progress indicators.
- Do not include the separate two-job animation tournament branch or the ongoing recursive animation matrix QA branch.
- Tag push is approved for this v0.1.2 release-prep task. GitHub Release text can reuse the same notes.

## Version And Docs Checks

- package.json version: `0.1.2`.
- package-lock.json root version: `0.1.2`.
- `docs/release/v0.1.2-release-notes.md`: added.
- `CHANGELOG.md`: v0.1.2 section added.
- `README.md`: v0.1.2 release materials added.
- `scripts/release-audit.mjs`: v0.1.2 release-prep files and version checks added.
- `src/App.tsx`: visible version badge updated to `v0.1.2`.
- `server/index.ts`: API health version updated to `0.1.2`.

## Automated Verification

Release-prep command set:

```powershell
npm run doctor
npm run typecheck
npm test
npm run build
npm run smoke
npm run release:audit
npm run ui:smoke
git diff --check
```

Results:

| Command | Result | Notes |
| --- | --- | --- |
| `npm run doctor` | passed | No hard setup failures found. |
| `npm run typecheck` | passed | TypeScript completed with no errors. |
| `npm test` | passed | 58 tests passed across 3 files. |
| `npm run build` | passed | Existing Vite chunk-size warning only. |
| `npm run smoke` | passed | Local smoke completed. |
| `npm run release:audit` | passed | v0.1.2 release notes and QA docs are covered. |
| `npm run ui:smoke` | passed | Browser UI smoke completed, including running job progress indicator coverage. |
| `git diff --check` | passed | CRLF normalization warnings only. |

## Included Reliability Evidence

- Animation delivery current baseline: `docs/qa/animation-delivery-reliability/current-baseline.md`.
- Running job progress indicator QA: `docs/qa/running-job-progress-indicator.md`.
- Detached direction split recovery QA: `docs/qa/animation-direction-split-completion-hotfix.md`.
- Settings recovery environment report QA: `docs/qa/settings-recovery-environment-report.md`.

## Tag Gate

Before tag approval, confirm:

- Main contains the release-prep commit.
- `package.json` and `package-lock.json` are both `0.1.2`.
- GitHub Actions main CI is successful or local release verification has passed.
- The final `docs/release/v0.1.2-release-notes.md` matches the actual tag commit range.
- Tag push / GitHub Release approval is explicit.
