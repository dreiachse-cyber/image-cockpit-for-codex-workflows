# v0.1.1 Release Prep QA

Date: 2026-06-27
Branch: `codex/v0-1-1-release-prep`
Status: verified on branch

## Scope

This QA note covers the v0.1.1 release-prep branch for version, release notes, changelog, release audit, and tag-readiness documentation.

Target commit before release-prep changes: `418dc74` (`Add image edit full-body fit safeguards`).

Release scope decision:

- Use the current main stabilization scope after `v0.1.0`.
- 027/028 dev-supervisor repair and bronze-candidate duplicate import prevention are not included because slot4 is still working on those changes.
- Tag push / GitHub Release publication remains owner-gated after main integration.

## Version And Docs Checks

- `package.json` version: `0.1.1`.
- `package-lock.json` root version: `0.1.1`.
- `docs/release/v0.1.1-release-notes.md`: added.
- `CHANGELOG.md`: v0.1.1 section added.
- `README.md`: safe/reset recovery links and v0.1.1 release materials added.
- `scripts/release-audit.mjs`: v0.1.1 release-prep files and version checks added.
- `src/App.tsx`: visible version badge updated to `v0.1.1`.

## Automated Verification

Executed release-prep commands:

```powershell
npm run doctor
npm run typecheck
npm test
npm run build
npm run smoke
npm run release:audit
npm run ui:smoke
git diff --check
```

Results:

| Command | Result | Notes |
| --- | --- | --- |
| `npm run doctor` | passed | No hard setup failures. Codex `--help` preflight completed through the terminal-runnable Codex path. |
| `npm run typecheck` | passed | `tsc --noEmit`. |
| `npm test` | passed | 42 tests passed across 3 files. |
| `npm run build` | passed | Existing Vite chunk-size warning only. |
| `npm run smoke` | passed | `Smoke passed.` |
| `npm run release:audit` | passed | Includes v0.1.1 version, release note, QA note, and app badge checks. |
| `npm run ui:smoke` | passed | First run timed out waiting for the initial workspace, then a clean rerun passed. |
| `git diff --check` | passed | No whitespace errors; Git reported LF-to-CRLF working-copy warnings only. |

Notes:

- Node-based direct syntax checks used the bundled Node runtime: `C:\Users\nakaya\.cache\codex-runtimes\codex-primary-runtime\dependencies\node\bin\node.exe`.
- `npm version 0.1.1 --no-git-tag-version` used the available PATH npm because the bundled runtime did not include `npm.cmd`.

## Browser Confirmation

Local browser checks were run against:

```text
http://127.0.0.1:5211/
```

API server:

```text
http://127.0.0.1:8811/
```

The in-app browser had Japanese saved as the UI language, so the primary tabs were verified using the visible Japanese labels.

| URL / Area | Result | Notes |
| --- | --- | --- |
| `/` | passed | Normal startup showed the cockpit, three workflow tabs, and visible version `v0.1.1`. |
| `/?safe=1` | passed | Safe recovery path opened and showed local state recovery content without loading heavy state. |
| `/reset-local-state.html` | passed | Reset page says it clears only Image Cockpit browser local state and never deletes repository files or `codex-handoff/outbox`. |
| Pixel Art Generation tab | passed | `ピクセルアートの生成` tab opened. |
| Image Editing tab | passed | `画像編集` tab opened. |
| Animation Generation tab | passed | `アニメーションの生成` tab opened. |
| Console errors | passed | Browser error log was empty. |

## Tag Gate

Do not push `v0.1.1` and do not create a GitHub Release from this branch without owner confirmation.

Before tag approval, confirm:

- Main contains the release-prep commit.
- `package.json` and `package-lock.json` are both `0.1.1`.
- GitHub Actions main CI is successful.
- The final `docs/release/v0.1.1-release-notes.md` matches the actual tag commit range.
- Tag push / GitHub Release approval is explicit.
