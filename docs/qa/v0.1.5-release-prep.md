# v0.1.5 Release Prep QA

Date: 2026-07-04

## Scope

This note covers the v0.1.5 release-prep update after completing the `codex/monster-girl-prompt-animation-batch` branch.

## Release approval

- Owner request: publish the completed Monster Girl Chibi prompt and animation QA work as `v0.1.5`.
- Main merge approval is explicit for this request.
- Tag push is approved for `v0.1.5` after release-prep verification.

## Target

- Source branch: `codex/monster-girl-prompt-animation-batch`
- Main update path: fast-forward or merge from the source branch into `main`
- Release tag: `v0.1.5`

## Version Updates

- package.json version: `0.1.5`
- package-lock.json root version: `0.1.5`
- App visible version badge: `v0.1.5`
- API health version: `0.1.5`
- Release notes: `docs/release/v0.1.5-release-notes.md`
- Prompt catalog: `docs/prompt-examples/monster-girl-prompts.md`

## Included Feature Evidence

- 20 Monster Girl Chibi prompt examples are present in the prompt catalog.
- 20 generated transparent source preview PNG files are present under `public/prompt-examples/monster-girl-*.png`.
- Actual browser prompt-modal verification showed 107 prompt cards, Monster Girl Chibi category count 20, and 20 Monster Girl Chibi images with no broken image loads.
- MG01-MG20 all reached accepted source-preview status.
- MG01-MG20 all reached accepted animation status for front idle, side idle, side run, side walk, and side attack.
- Accepted animation coverage is 100 slots, represented by 80 unique generated animation jobs because front idle and side idle share the accepted five-direction idle sheet per character.
- `codex-handoff/`, generated outbox files, large local QA contact sheets, and temporary review galleries are intentionally not included in the release payload.

## Validation Commands

| Command | Result | Note |
| --- | --- | --- |
| `npm run doctor` | passed | Bundled Node equivalent, no hard setup failures. |
| `npm run typecheck` | passed | `tsc --noEmit`. |
| `npm test` | passed | 3 files / 61 tests passed. |
| `npm run build` | passed | `tsc -b` and `vite build`; existing chunk-size warning only. |
| `npm run smoke` | passed | Smoke script passed. |
| `npm run release:audit` | passed | v0.1.5 release docs, version checks, and Monster Girl Chibi prompt assets passed. |
| `npm run ui:smoke` | passed | Browser UI smoke passed, including Prompt Examples count and Monster Girl Chibi category coverage. |
| `git diff --check` | passed | No whitespace errors; CRLF normalization warnings only. |

## Tag Checklist

- Confirm `main` contains the Monster Girl Chibi prompt catalog and v0.1.5 release-prep updates.
- Commit this v0.1.5 release-prep update.
- Push `main`.
- Create annotated tag `v0.1.5` on the release-prep commit.
- Push tag `v0.1.5`.
